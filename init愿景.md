# 🛩️ Striker — 无人机搜查打击飞控自动飞行系统 · 项目愿景

> **文档版本**: v2.1 · **日期**: 2026-04-12 · **状态**: 探索完成 → 待 OpenSpec 提议
>
> 本文档是从零开始的架构探索产物，记录了所有技术调研、决策推演和架构设计。
> 它将作为后续 OpenSpec proposal / design / specs 的权威输入源。

---

# Part 0: 项目概述

## 项目使命

建立一套完整的固定翼无人机自动飞行控制程序，作为「搜查打击」系统中飞控自动化环节的核心。
程序运行在机载伴飞计算机（树莓派5 / NVIDIA Orin Super）上，通过 MAVLink 协议与 CUAV X7 飞控通信，
实现从起飞到投弹到降落的全自主飞行任务。

## 任务全景 — 完整的飞行生命周期

```
  ┌─────────────────────────────────────────────────────────────────────────────┐
  │                        MISSION LIFECYCLE (全任务生命周期)                       │
  └─────────────────────────────────────────────────────────────────────────────┘

  ┌──────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────┐
  │      │    │          │    │          │    │          │    │          │    │      │
  │ ARM  │───▶│ TAKEOFF  │───▶│ LOITER   │───▶│ NAVIGATE │───▶│ STRIKE   │───▶│ RTL  │
  │      │    │          │    │ (等待坐标) │    │ (飞向目标) │    │ (投弹)    │    │ LAND │
  └──────┘    └──────────┘    └──────────┘    └──────────┘    └──────────┘    └──────┘
     │             │               │  ▲            │               │             │
     │             │               │  │            │               │             │
     ▼             ▼               ▼  │            ▼               ▼             ▼
  预检查        爬升到           盘旋等待     收到坐标       解算投弹点        飞向跑道
  安全检查      指定高度         视觉解算     退出盘旋       操控舵机释放      进近降落
  GPS锁定                       结果传入     航线规划       确认投弹          减速着陆
```

## 硬件架构拓扑

```
  ┌─────────────────────────────────────────────────────────────────────────┐
  │                          机载系统拓扑                                     │
  │                                                                         │
  │   ┌─────────────────┐         UART/Serial          ┌────────────────┐  │
  │   │  CUAV X7 飞控    │  ◄──────────────────────────▶ │ 树莓派5 / Orin │  │
  │   │  (ArduPlane)     │    MAVLink 2.0 @ 921600     │ (Companion PC)  │  │
  │   │                  │                              │                 │  │
  │   │  • 姿态控制       │                              │ • 任务状态机     │  │
  │   │  • PID 稳定      │                              │ • MAVLink通信   │  │
  │   │  • RC Failsafe   │         PWM/Servo            │ • 投弹解算      │  │
  │   │  • GPS 导航      │  ──────────────────────────▶ │ • 视觉链路接口   │  │
  │   │  • 高度/空速控制  │                              │ • 日志/遥测     │  │
  │   └─────────────────┘                              └────────┬────────┘  │
  │          │                                                   │          │
  │          │ PWM                                               │ TCP/UDP  │
  │          ▼                                                   ▼          │
  │   ┌──────────────┐                                  ┌──────────────┐   │
  │   │  投弹舵机     │                                  │  视觉链路     │   │
  │   │  释放机构     │                                  │ (目标坐标源)  │   │
  │   └──────────────┘                                  └──────────────┘   │
  │                                                                         │
  │          ┌──────────────┐                                               │
  │          │  地面站 GCS   │  ◄───── WiFi/Telemetry ──────────────────── │
  │          │  (监控/应急)   │                                              │
  │          └──────────────┘                                               │
  └─────────────────────────────────────────────────────────────────────────┘
```

## CUAV X7 飞控关键参数

| 项目 | 值 |
|------|----|
| 处理器 | STM32H743, Cortex-M7 @ 480MHz |
| 固件 | ArduPlane (ArduPilot) |
| PWM 输出 | 14路 (12路支持 DShot) |
| 串口 | SERIAL1 (TELEM1), SERIAL2 (TELEM2) 可用于 Companion |
| SERIAL2 推荐波特率 | 921600 |
| GPS | 双 GPS 端口 |
| CAN 总线 | 2路 DroneCAN |
| I2C | 4路 (2路外部) |
| 电源监控 | 1 模拟 + 1 DroneCAN |

---

# Part 1: 技术选型

## 1.1 编程语言

```
  ┌─────────────────────────────────────────────────────────────────┐
  │                    语言选择比较矩阵                                │
  ├──────────────┬──────────┬──────────┬──────────┬────────────────┤
  │              │ Python   │ C++      │ Rust     │ Python+C ext   │
  ├──────────────┼──────────┼──────────┼──────────┼────────────────┤
  │ MAVLink生态  │ ★★★★★   │ ★★★★    │ ★★      │ ★★★★★         │
  │ 开发效率     │ ★★★★★   │ ★★      │ ★★★     │ ★★★★          │
  │ 运行性能     │ ★★★     │ ★★★★★  │ ★★★★★  │ ★★★★          │
  │ 跨平台兼容   │ ★★★★★   │ ★★★★    │ ★★★★    │ ★★★★          │
  │ 团队上手     │ ★★★★★   │ ★★      │ ★★      │ ★★★★          │
  │ 实时性       │ ★★★     │ ★★★★★  │ ★★★★★  │ ★★★★          │
  │ RPi5兼容     │ ★★★★★   │ ★★★★    │ ★★★★    │ ★★★★★         │
  │ Orin兼容     │ ★★★★★   │ ★★★★★  │ ★★★★    │ ★★★★★         │
  └──────────────┴──────────┴──────────┴──────────┴────────────────┘
```

**✅ 决策: Python 3.13**

- pymavlink 是 ArduPilot 官方维护的 Python MAVLink 库，生态最完善
- RPi5 和 Orin 都原生支持，无需交叉编译
- 本程序是任务决策层（Task-level decision），延迟容忍度 10-50ms，Python 足够
- 迭代速度快，方便现场调试
- 异步框架使用 asyncio 原生协程

## 1.2 MAVLink 通信库

```
  ┌─────────────────────────────────────────────────────────────────┐
  │               MAVLink 库选择                                     │
  ├────────────────┬───────────────────────────────────────────────┤
  │                │                                               │
  │  pymavlink     │  ✅ ArduPilot 官方库                           │
  │  (✅ 选定)     │  ✅ 底层控制，完全访问所有 MAVLink 消息          │
  │                │  ✅ 零额外依赖，轻量                            │
  │                │  ✅ ArduPilot 社区主力维护                      │
  │                │  ⚠️ API 较底层，需要封装                        │
  │                │                                               │
  ├────────────────┼───────────────────────────────────────────────┤
  │                │                                               │
  │  MAVSDK-Python │  ✅ 高级 API，开发友好                         │
  │  (❌ 排除)     │  ✅ 原生 async/await 支持                      │
  │                │  ⚠️ 主要面向 PX4，ArduPilot 支持不完整          │
  │                │  ❌ CUAV X7 跑 ArduPlane，有兼容风险            │
  │                │                                               │
  ├────────────────┼───────────────────────────────────────────────┤
  │                │                                               │
  │  DroneKit      │  ⚠️ 已停止主要维护                              │
  │  (❌ 排除)     │  ❌ 不建议新项目使用                             │
  │                │                                               │
  └────────────────┴───────────────────────────────────────────────┘
```

**✅ 决策: pymavlink + 自建高级封装层**

## 1.3 软件分层架构

```
  ┌─────────────────────────────────────────────────────────────────────────┐
  │                        SOFTWARE ARCHITECTURE                           │
  │                        (四层架构)                                       │
  ├─────────────────────────────────────────────────────────────────────────┤
  │                                                                         │
  │  Layer 4: Mission Orchestrator (任务编排层)                               │
  │  ┌─────────────────────────────────────────────────────────────────┐   │
  │  │  MissionStateMachine — 有限状态机 + 事件驱动                     │   │
  │  │  INIT → PREFLIGHT → TAKEOFF → LOITER → ENROUTE → ...           │   │
  │  └─────────────────────────────────────────────────────────────────┘   │
  │                              │ 发出命令                                  │
  │                              ▼                                          │
  │  Layer 3: Flight Command (飞行指令层)                                    │
  │  ┌─────────────────────────────────────────────────────────────────┐   │
  │  │  FlightController — 封装高级飞行操作                              │   │
  │  │  arm() / takeoff(alt) / goto(lat,lon,alt) / set_mode() / ...   │   │
  │  └─────────────────────────────────────────────────────────────────┘   │
  │                              │ 发送MAVLink                               │
  │                              ▼                                          │
  │  Layer 2: MAVLink Adapter (通信适配层)                                   │
  │  ┌─────────────────────────────────────────────────────────────────┐   │
  │  │  MAVLinkConnection — 消息收发、心跳、连接健康监控                  │   │
  │  └─────────────────────────────────────────────────────────────────┘   │
  │                              │ 串口/UDP                                  │
  │                              ▼                                          │
  │  Layer 1: Transport (传输层)                                            │
  │  ┌─────────────────────────────────────────────────────────────────┐   │
  │  │  Serial: /dev/serial0 @ 921600 (RPi5)                          │   │
  │  │  Serial: /dev/ttyTHS1  (Orin — 需要适配)                        │   │
  │  │  UDP: 127.0.0.1:14550  (SITL 模拟测试)                         │   │
  │  └─────────────────────────────────────────────────────────────────┘   │
  │                                                                         │
  │  Cross-cutting: Safety Monitor │ Telemetry Logger │ Config Manager      │
  │                 Vision Interface │ Ballistic Calculator                  │
  └─────────────────────────────────────────────────────────────────────────┘
```

---

# Part 2: 固定翼精化架构

## 固定翼的本质差异 — 这改变了一切

你说「仅固定翼」，这是一个非常重要的约束。固定翼和多旋翼在自动飞行控制上有根本性区别：

```
  多旋翼 (Copter)                        固定翼 (Plane)
  ═══════════════                        ═══════════════

  • 可以悬停                              • 不能悬停，必须持续飞行
  • 可以原地起降                           • 需要跑道起飞/降落
  • LOITER = 原地悬停                      • LOITER = 以某点为圆心画圈盘旋
  • GUIDED goto = 飞到点然后停              • GUIDED goto = 飞到点然后继续绕
  • 投弹可以悬停在正上方                    • 投弹必须在飞行中释放 (弹道计算)
  • LAND = 原地垂直降落                    • LAND = 需要进近航线 + 下滑道
  • 速度独立于飞行                          • 失速 = 坠毁 (有最低速度约束)
```

这意味着你的状态机和投弹逻辑必须按照固定翼的物理特性来设计。

---

## ArduPlane 飞行模式映射

根据我从 ArduPilot 官方文档挖到的信息，你的任务各阶段需要用到的 ArduPlane 模式：

```
  ┌────────────────────────────────────────────────────────────────────────┐
  │              ArduPlane 模式 ↔ 任务阶段映射                              │
  ├─────────────┬──────────────────┬───────────────────────────────────────┤
  │ 任务阶段     │ ArduPlane 模式    │ MAVLink 实现                         │
  ├─────────────┼──────────────────┼───────────────────────────────────────┤
  │ 起飞        │ AUTO             │ NAV_TAKEOFF 任务项 (需要设定爬升高度    │
  │             │ (含TAKEOFF航点)   │   和最小空速)                          │
  ├─────────────┼──────────────────┼───────────────────────────────────────┤
  │ 盘旋等待    │ LOITER           │ MAV_CMD_NAV_LOITER_UNLIM              │
  │             │                  │ 飞机以 WP_LOITER_RAD 为半径画圈        │
  │             │                  │ 保持进入盘旋时的高度                    │
  ├─────────────┼──────────────────┼───────────────────────────────────────┤
  │ 飞向目标    │ GUIDED           │ MISSION_ITEM_INT (current=2)          │
  │             │                  │ MAV_CMD_NAV_WAYPOINT                  │
  │             │                  │ MAV_CMD_DO_CHANGE_SPEED 控制巡航速度   │
  ├─────────────┼──────────────────┼───────────────────────────────────────┤
  │ 投弹进近    │ GUIDED           │ MISSION_ITEM_INT → 投弹释放点          │
  │             │                  │  (保持稳定高度和航向)                  │
  ├─────────────┼──────────────────┼───────────────────────────────────────┤
  │ 投弹释放    │ GUIDED (维持)    │ MAV_CMD_DO_SET_SERVO                  │
  │             │                  │  (通道号, PWM脉宽)                    │
  ├─────────────┼──────────────────┼───────────────────────────────────────┤
  │ 返航        │ RTL              │ MAV_CMD_NAV_RETURN_TO_LAUNCH          │
  │             │                  │ 或 GUIDED → 跑道进近点                │
  ├─────────────┼──────────────────┼───────────────────────────────────────┤
  │ 降落        │ AUTO             │ 预设 DO_LAND_START + NAV_LAND         │
  │             │ (含LAND序列)     │ 需要进近航点 + 着陆点 + 下滑道参数     │
  │             │ 或 AUTOLAND      │ (ArduPlane 4.5+ 新增 AUTOLAND 模式)   │
  ├─────────────┼──────────────────┼───────────────────────────────────────┤
  │ 人工接管    │ MANUAL/FBWA/FBWB │ RC Override 信号 (永远最高优先级)      │
  └─────────────┴──────────────────┴───────────────────────────────────────┘
```

> **关键发现**：ArduPlane 的自动降落比较复杂，需要预先在 Auto 任务里写好 `DO_LAND_START` → 进近航点 → `NAV_LAND` 的序列。或者用 ArduPlane 4.5+ 新增的 `AUTOLAND` 模式。我们的代码需要在起飞前就把降落序列写入飞控。

---

## 精化状态机 (固定翼版)

```
  ┌─────────────────────────────────────────────────────────────────────────┐
  │            FIXED-WING MISSION STATE MACHINE v2                          │
  └─────────────────────────────────────────────────────────────────────────┘

                         ┌────────────┐
                         │    INIT    │
                         │  系统初始化  │
                         │            │
                         │ • 加载配置  │
                         │ • 连接飞控  │
                         │ • 连接视觉  │
                         └─────┬──────┘
                               │ 所有连接就绪
                               ▼
                         ┌────────────┐
                         │ PREFLIGHT  │
                         │  预飞检查   │
                         │            │
                         │ • GPS 3D锁 │
                         │ • 电池 OK   │
                         │ • 空速管 OK │  ← 固定翼特有！
                         │ • 舵面检查  │  ← 固定翼特有！
                         │ • 降落序列  │  ← 预写入飞控
                         │   已上传    │
                         └─────┬──────┘
                               │ 预检通过 + ARM
                               ▼
                         ┌────────────┐
                         │  TAKEOFF   │
                         │  自动起飞   │
                         │            │
                         │ • AUTO模式 │
                         │ • 执行      │
                         │  NAV_TAKEOFF│
                         │ • 爬升到    │
                         │  巡航高度   │
                         └─────┬──────┘
                               │ 到达巡航高度
                               ▼
                         ┌────────────┐  ◄──────────────────────┐
                         │  LOITER    │                          │
                         │  盘旋等待   │   目标坐标更新           │
                         │            │   (自适应频率 fallback)  │
                         │ • LOITER   │                          │
                         │   模式     │                          │
                         │ • 等待视觉 │                          │
                         │   坐标传入 │                          │
                         └─────┬──────┘                          │
                               │ 收到有效目标坐标                  │
                               ▼                                  │
                         ┌────────────┐                          │
                         │  ENROUTE   │                          │
                         │  航向目标   │  如果飞行中收到新坐标      │
                         │            │  → 更新目标，重新导航 ────┘
                         │ • GUIDED   │    (自适应频率)
                         │ • 飞向目标  │
                         │   区域     │
                         │ • 速度控制 │
                         └─────┬──────┘
                               │ 进入目标区域 (< approach_radius)
                               ▼
                         ┌────────────┐
                         │ APPROACH   │
                         │  投弹进近   │
                         │            │
                         │ • 稳定航向 │
                         │ • 稳定高度 │
                         │ • 弹道解算 │
                         │   持续更新 │
                         │ • 风速补偿 │
                         └─────┬──────┘
                               │ 到达计算出的投弹释放点
                               ▼
                         ┌────────────┐
                         │  RELEASE   │
                         │  投弹释放   │
                         │            │
                         │ • DO_SET   │
                         │   _SERVO   │
                         │   或GPIO   │
                         │ • 确认触发 │
                         └─────┬──────┘
                               │ 释放确认
                               ▼
                         ┌────────────┐
                         │  RETURN    │
                         │  返航      │
                         │            │
                         │ • GUIDED→  │
                         │  跑道进近点 │
                         │ • 或 RTL   │
                         └─────┬──────┘
                               │ 接近跑道
                               ▼
                         ┌────────────┐
                         │  LANDING   │
                         │  自动降落   │
                         │            │
                         │ • 切AUTO   │
                         │ • 执行预存  │
                         │  降落序列   │
                         │ • DO_LAND  │
                         │  _START    │
                         └─────┬──────┘
                               │ 落地 + DISARM
                               ▼
                         ┌────────────┐
                         │ COMPLETED  │
                         │ 任务完成    │
                         │            │
                         │ • 日志保存  │
                         │ • 遥测归档  │
                         └────────────┘


  ══════════════════════════════════════════════════════════════════════════
  全局中断 (任意状态均可触发)
  ══════════════════════════════════════════════════════════════════════════

                    ┌─────────────────────────────┐
  任意状态 ────────▶│       OVERRIDE              │
                    │    人工接管 (最高优先级)      │
                    │                             │
                    │ • RC检测到手动模式切换        │
                    │ • GCS发送OVERRIDE命令        │
                    │ • 本程序立即让出控制权        │
                    │ • 停止所有自动行为            │
                    │ • 记录接管时的状态和坐标      │
                    │                             │
                    │ 结果: 任务终止               │
                    └─────────────────────────────┘

                    ┌─────────────────────────────┐
  任意状态 ────────▶│       EMERGENCY             │
                    │    紧急安全降落               │
                    │                             │
                    │ 触发条件:                    │
                    │ • 电池电压 < 阈值            │
                    │ • MAVLink 心跳丢失 > N秒     │
                    │ • GPS 信号丢失              │
                    │ • 空速异常 (失速/超速警告)   │
                    │ • 地理围栏越界              │
                    │                             │
                    │ 动作: 切RTL或LAND           │
                    └─────────────────────────────┘
```

---

## 三层配置优先级系统

你要求 `default → config.json → ENV` 三层优先级。这和 `pydantic-settings` 天然吻合：

```
  ┌─────────────────────────────────────────────────────────────────┐
  │              三层配置优先级 (低 → 高)                              │
  ├─────────────────────────────────────────────────────────────────┤
  │                                                                 │
  │  Layer 1: 代码内默认值 (lowest)                                  │
  │  ┌───────────────────────────────────────────────────────────┐ │
  │  │  class StrikerSettings(BaseSettings):                     │ │
  │  │      serial_port: str = "/dev/serial0"                    │ │
  │  │      serial_baud: int = 921600                            │ │
  │  │      loiter_radius_m: float = 80.0                        │ │
  │  │      release_servo_channel: int = 10                      │ │
  │  │      release_servo_pwm_open: int = 1900                   │ │
  │  │      ...                                                  │ │
  │  └───────────────────────────────────────────────────────────┘ │
  │                         ▲ 被覆盖                                │
  │                         │                                       │
  │  Layer 2: config.json 文件                                      │
  │  ┌───────────────────────────────────────────────────────────┐ │
  │  │  {                                                        │ │
  │  │    "serial_port": "/dev/ttyTHS1",        ← Orin用这个     │ │
  │  │    "loiter_radius_m": 60.0,                               │ │
  │  │    "release_method": "gpio",             ← 切换到GPIO    │ │
  │  │    "landing_approach_alt_m": 30.0                         │ │
  │  │  }                                                        │ │
  │  └───────────────────────────────────────────────────────────┘ │
  │                         ▲ 被覆盖                                │
  │                         │                                       │
  │  Layer 3: 环境变量 (highest)                                    │
  │  ┌───────────────────────────────────────────────────────────┐ │
  │  │  STRIKER_SERIAL_PORT=/dev/ttyUSB0     ← 临时调试串口      │ │
  │  │  STRIKER_RELEASE_METHOD=mavlink       ← 临时切回MAVLink  │ │
  │  │  STRIKER_DRY_RUN=true                 ← 模拟模式，不真投  │ │
  │  └───────────────────────────────────────────────────────────┘ │
  │                                                                 │
  │  pydantic-settings 原生支持这个优先级链:                         │
  │  model_config = SettingsConfigDict(                             │
  │      env_prefix="STRIKER_",                                     │
  │      json_file="config.json",                                   │
  │      json_file_encoding="utf-8",                                │
  │  )                                                              │
  │                                                                 │
  │  加载顺序: init defaults → json_file → env_vars                 │
  │  (后面的覆盖前面的)                                               │
  └─────────────────────────────────────────────────────────────────┘
```

---

## 视觉链路接口 — GPS 绝对坐标 + 坐标转换工具库

> **核心决策 (v2.1 更新)**:
> 飞控程序**只接受 GPS 绝对坐标** (lat/lon/alt WGS84)。
> 坐标转换算法（相对坐标→GPS、地图坐标→GPS）作为**独立工具库**暴露公共接口，
> 供视觉链路或其他上游系统在发送坐标前自行调用。
> 这样做保证了飞控程序的简洁性和确定性，同时为第三方提供了可复用的转换能力。

```
  ┌─────────────────────────────────────────────────────────────────────┐
  │                    VISION INTERFACE DESIGN (v2.1)                    │
  └─────────────────────────────────────────────────────────────────────┘

  ┌─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
  │         上游系统边界 (不属于本飞控程序, 但由本项目提供工具库)          │
  │                                                                     │
  │  视觉进程 / 外部系统                   striker.utils.geo (工具库)    │
  │  ┌──────────────────────┐             ┌───────────────────────┐    │
  │  │                      │  相对坐标    │  CoordConverter       │    │
  │  │  目标识别/解算        │  ──────────▶│  (公共接口, 可独立使用) │    │
  │  │                      │             │                       │    │
  │  │  原始输出可能是:      │             │  .relative_to_gps()   │    │
  │  │  • 地图相对坐标       │             │  .map_pixel_to_gps()  │    │
  │  │  • 像素坐标+地图      │             │  .validate_gps()      │    │
  │  │  • 已知GPS坐标        │  直接GPS    │                       │    │
  │  │                      │  ─ ─ ─ ─ ┐ │  输入: 相对/地图坐标   │    │
  │  └──────────────────────┘           │ │  输出: GPS绝对坐标     │    │
  │                                     │ └───────────┬───────────┘    │
  │                                     │             │                │
  └─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┼ ─ ─ ─ ─ ─ ─│─ ─ ─ ─ ─ ─ ─ ┘
                                        │             │
  ══════════════════════════════════════╪═════════════╪════════════════
  飞控程序边界 (只接受GPS绝对坐标)        │             │
  ══════════════════════════════════════╪═════════════╪════════════════
                                        │             │
                                        ▼             ▼
                              ┌──────────────────────────────┐
                              │       VisionReceiver         │
                              │       (Protocol接口)          │
                              │                              │
                              │  唯一接受格式 (GPS绝对坐标):  │
                              │  {                            │
                              │    "lat_deg": 30.xxxxxx,     │
                              │    "lon_deg": 120.xxxxxx,    │
                              │    "alt_m": 0.0,     (可选)  │
                              │    "confidence": 0.92         │
                              │  }                            │
                              │                              │
                              │  校验: lat ∈ [-90,90]         │
                              │        lon ∈ [-180,180]       │
                              │        confidence > 阈值      │
                              │  不合法 → 拒绝 + WARNING日志  │
                              │                              │
                              │  传输实现:                    │
                              │  • TcpReceiver               │
                              │  • UdpReceiver               │
                              │  • SharedMemReceiver (预留)   │
                              └──────────────┬───────────────┘
                                             │
                                             ▼
                              ┌──────────────────────────────┐
                              │       TargetTracker          │
                              │       (自适应频率)            │
                              └──────────────────────────────┘

  ═══════════════════════════════════════════════════════════════════
  坐标转换工具库 (striker.utils.geo.CoordConverter)
  ═══════════════════════════════════════════════════════════════════

  独立可用, 不依赖飞控运行时, 第三方可直接调用:

  ┌──────────────────────────────────────────────────────────────┐
  │  class CoordConverter:                                       │
  │      """坐标转换工具 — 供视觉链路或其他上游系统使用"""         │
  │                                                              │
  │      @staticmethod                                           │
  │      def relative_to_gps(                                    │
  │          base_lat_deg, base_lon_deg,                         │
  │          offset_north_m, offset_east_m,                      │
  │      ) -> tuple[float, float]:                               │
  │          """NED 相对偏移 → GPS 绝对坐标"""                    │
  │                                                              │
  │      @staticmethod                                           │
  │      def map_pixel_to_gps(                                   │
  │          pixel_x, pixel_y,                                   │
  │          map_origin_lat_deg, map_origin_lon_deg,             │
  │          map_scale_m_per_pixel, map_rotation_deg,            │
  │      ) -> tuple[float, float]:                               │
  │          """地图像素坐标 → GPS (含旋转和缩放)"""              │
  │                                                              │
  │      @staticmethod                                           │
  │      def validate_gps(lat_deg, lon_deg) -> bool:             │
  │          """校验 GPS 坐标合法性"""                            │
  └──────────────────────────────────────────────────────────────┘
```

### 自适应频率策略

```
  视觉传入频率           程序行为
  ─────────────          ──────────────────────────────────
  0 Hz (无数据)          LOITER 状态持续盘旋等待

  单次传入 (1次)         立即采纳坐标，切换到 ENROUTE

  低频 (< 1 Hz)          每次收到都更新目标，ENROUTE中修正航线

  高频 (> 1 Hz)          节流: 取最近 N 帧做加权平均
                          避免频繁改变航向导致飞行不稳

  实现方式: 滑动窗口 + 最新坐标始终可用
  ┌──────────────────────────────────────────────────┐
  │  class TargetTracker:                            │
  │      last_target: GpsTarget | None               │
  │      update_window: deque[GpsTarget]  # 最近N个  │
  │      last_update_time: float                     │
  │                                                  │
  │      @property                                   │
  │      def effective_target(self) -> GpsTarget:     │
  │          # 单次 → 直接返回                        │
  │          # 多次 → 加权平均 (最新权重最高)          │
  │          # 过期 → 返回最后有效值 + 标记stale      │
  └──────────────────────────────────────────────────┘
```

---

## 投弹释放 — 双通道抽象

```
  ┌─────────────────────────────────────────────────────────────────┐
  │               RELEASE MECHANISM ABSTRACTION                      │
  └─────────────────────────────────────────────────────────────────┘

                  ┌─────────────────────────┐
                  │   ReleaseController     │
                  │   (Protocol/ABC)        │
                  │                         │
                  │   + arm()               │
                  │   + release()           │
                  │   + is_armed: bool      │
                  │   + is_released: bool   │
                  └───────┬─────────────────┘
                          │
              ┌───────────┴───────────┐
              │                       │
              ▼                       ▼
  ┌───────────────────┐   ┌───────────────────┐
  │ MavlinkServo      │   │ GpioServo         │
  │ Release           │   │ Release           │
  │                   │   │                   │
  │ 通过飞控PWM输出   │   │ 通过树莓派GPIO    │
  │ DO_SET_SERVO      │   │ 直接驱动          │
  │ channel + PWM     │   │ RPi.GPIO / gpiod  │
  │                   │   │  或 Orin GPIO     │
  └───────────────────┘   └───────────────────┘

  选择通过 config.json 配置:
  {
    "release_method": "mavlink",    // 或 "gpio"
    "release_servo_channel": 10,    // mavlink用
    "release_servo_pwm_open": 1900,
    "release_servo_pwm_close": 1100,
    "release_gpio_pin": 17,         // gpio用
    "release_gpio_active_high": true
  }

  预留扩展接口: 未来可以实现更复杂的时序释放
  ┌─────────────────────────────────────┐
  │  class SequencedRelease(Release):   │
  │      steps: list[ReleaseStep]       │
  │      # step1: 解锁保险 (servo1→90°)│
  │      # step2: 等待 200ms            │
  │      # step3: 释放 (servo2→180°)   │
  │      # step4: 等待 100ms            │
  │      # step5: 回位                  │
  └─────────────────────────────────────┘
```

---

## 固定翼投弹弹道解算 — 关键物理模型

固定翼投弹是这个项目最核心的算法挑战。因为飞机不能悬停，必须在飞行中释放，物体会沿抛物线下落：

```
  ┌─────────────────────────────────────────────────────────────────────┐
  │              FIXED-WING BALLISTIC DROP MODEL                        │
  └─────────────────────────────────────────────────────────────────────┘

  侧视图:
                               v_ground
              飞机 ═══════════════════════▶  飞行方向
               ╲ 释放点 (release point)
                ╲
                 ╲  载荷自由落体
                  ╲  + 初始水平速度 v_ground
                   ╲  + 风的影响
                    ╲
                     ╲  h (释放高度)
                      ╲
                       ╲
                        ●  目标 (target)
             ◄──────────►
                d_forward (沿飞行方向提前距离)

  基础公式:
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  下落时间:    t_fall = √(2h / g)
  
  其中 g ≈ 9.81 m/s²

  提前距离(无风): d = v_ground × t_fall

  含风补偿:
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  d_x = (v_ground_x + W_x) × t_fall
  d_y = (v_ground_y + W_y) × t_fall

  其中 W_x, W_y 是风速分量
  (ArduPilot EKF 会输出 WIND 消息，包含 wind_speed 和 wind_dir)

  考虑空气阻力 (增强模型):
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  t_fall = 更复杂，需要数值积分
  实际下落时间 > 理想自由落体 (因为空阻减慢下落)
  水平距离更短 (空阻让水平速度衰减)

  这里建议: 先用无阻力模型上线，然后通过实飞标定修正系数

  释放坐标计算 (地理坐标系):
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  release_lat = target_lat - (d_north / R_earth) × (180/π)
  release_lon = target_lon - (d_east / R_earth / cos(lat)) × (180/π)

  需要的遥测数据:
  ┌───────────────────────────────────────────────┐
  │  GLOBAL_POSITION_INT → lat, lon, relative_alt │
  │  VFR_HUD             → groundspeed, heading   │
  │  WIND                → wind_speed, wind_dir    │
  │  ATTITUDE            → roll, pitch, yaw        │
  └───────────────────────────────────────────────┘
```

---

## 人工 Override — 设计哲学

```
  ┌─────────────────────────────────────────────────────────────────┐
  │            OVERRIDE 优先级模型                                    │
  ├─────────────────────────────────────────────────────────────────┤
  │                                                                 │
  │  优先级 (高→低):                                                 │
  │                                                                 │
  │  ┌──────────────────────────────────────┐                      │
  │  │  1. RC 遥控器 (物理硬件)             │ ◄── 永远最高          │
  │  │     飞控固件级别的安全保障             │     本程序无法          │
  │  │     即使本程序崩溃也能接管            │     也不应干预          │
  │  └──────────────────────────────────────┘                      │
  │  ┌──────────────────────────────────────┐                      │
  │  │  2. GCS Override 命令                │ ◄── 地面站发来的      │
  │  │     OVERRIDE / MODE_CHANGE           │     手动接管信号      │
  │  │     本程序检测到后立即让出            │                      │
  │  └──────────────────────────────────────┘                      │
  │  ┌──────────────────────────────────────┐                      │
  │  │  3. Safety Monitor                  │ ◄── 本程序安全模块    │
  │  │     电池/GPS/围栏 等紧急情况          │     自动切RTL         │
  │  └──────────────────────────────────────┘                      │
  │  ┌──────────────────────────────────────┐                      │
  │  │  4. Mission State Machine           │ ◄── 正常任务流程      │
  │  │     自动飞行控制                     │                      │
  │  └──────────────────────────────────────┘                      │
  │                                                                 │
  │  关键原则:                                                      │
  │  • 本程序在每个控制循环都检查飞控当前模式                        │
  │  • 如果模式不是程序设定的 → 说明被接管 → 立即停止所有操作        │
  │  • 程序进入 OVERRIDE 终态，记录日志，等待人工处理               │
  │  • 不自动恢复自动飞行 (安全第一)                                │
  └─────────────────────────────────────────────────────────────────┘
```

---

## Python 3.13 特性利用

Python 3.13 (2024年10月发布) 是当前最新稳定版本，已充分验证：

```
  ┌─────────────────────────────────────────────────────────────────┐
  │              Python 3.13 项目相关特性                             │
  ├──────────────────────┬──────────────────────────────────────────┤
  │ 改进错误消息          │ 更精确的回溯定位，方便现场调试            │
  ├──────────────────────┼──────────────────────────────────────────┤
  │ 实验性 JIT           │ 可选的 JIT 编译器 (--enable-experimental  │
  │                      │ -jit)，可能提升热循环性能                │
  ├──────────────────────┼──────────────────────────────────────────┤
  │ 改进 typing          │ TypeVar defaults (PEP 696)，简化泛型     │
  ├──────────────────────┼──────────────────────────────────────────┤
  │ asyncio 改进         │ 更可靠的 TaskGroup 和取消语义             │
  ├──────────────────────┼──────────────────────────────────────────┤
  │ ✅ pymavlink 兼容    │ pymavlink 完全兼容 3.13，无 wheel 问题    │
  │                      │ (3.14 存在 fastcrc wheel 兼容风险)       │
  └──────────────────────┴──────────────────────────────────────────┘
```

---

## 核心运行时架构 (asyncio)

```
  ┌─────────────────────────────────────────────────────────────────────┐
  │                    ASYNCIO EVENT LOOP                                │
  │                    (单进程, 多协程)                                   │
  ├─────────────────────────────────────────────────────────────────────┤
  │                                                                     │
  │  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐       │
  │  │ mavlink_loop   │  │ vision_loop    │  │ safety_loop    │       │
  │  │                │  │                │  │                │       │
  │  │ while True:    │  │ while True:    │  │ while True:    │       │
  │  │   msg=recv()   │  │   data=recv()  │  │   check_batt() │       │
  │  │   dispatch(msg)│  │   parse(data)  │  │   check_gps()  │       │
  │  │   send_hb()    │  │   update_      │  │   check_fence()│       │
  │  │                │  │    tracker()   │  │   check_link() │       │
  │  │ 20-50 Hz       │  │   emit_event() │  │   check_mode() │ ← override检测
  │  └───────┬────────┘  └───────┬────────┘  └───────┬────────┘       │
  │          │                   │                    │                 │
  │          └───────────────────┼────────────────────┘                 │
  │                              │                                      │
  │                              ▼ events                               │
  │                    ┌──────────────────┐                             │
  │                    │ MissionFSM       │                             │
  │                    │                  │                             │
  │                    │ process_event()  │                             │
  │                    │ current_state    │                             │
  │                    │   .on_enter()    │                             │
  │                    │   .execute()     │                             │
  │                    │   .on_exit()     │                             │
  │                    └──────────────────┘                             │
  │                              │                                      │
  │                              ▼ commands                             │
  │                    ┌──────────────────┐                             │
  │                    │ FlightController │                             │
  │                    │                  │                             │
  │                    │ arm()            │                             │
  │                    │ set_mode()       │                             │
  │                    │ goto()           │                             │
  │                    │ set_speed()      │                             │
  │                    │ trigger_release()│                             │
  │                    └──────────────────┘                             │
  │                                                                     │
  │  所有协程通过 asyncio.Queue 和 asyncio.Event 通信                    │
  │  零线程，零锁 — 固有线程安全                                         │
  └─────────────────────────────────────────────────────────────────────┘
```

---

# Part 3: AI 治理架构 — 三层渐进式治理

> **核心原则 (v2.1)**:
> 随着代码库增长，规范必然膨胀。将所有规范塞进一个 AGENTS.md 会导致
> AI「指令预算」溢出 (研究表明 ~150-200 条高质量指令是 LLM 有效跟踪上限)。
>
> **解决方案**: 采用 **Progressive Disclosure（渐进披露）** 模式——
> 顶层 AGENTS.md 只保留宪法级 (<100行) 规范 + Skill 路由表，
> 模块级规范作为标准 Skill 按需加载。

## 三层治理架构总览

```
  ┌─── Tier 1: AGENTS.md  ──  宪法级 (永远生效, <100行) ─────────────────────┐
  │                                                                           │
  │  • 项目身份 (Striker = 固定翼无人机飞控自动飞行系统)                        │
  │  • 技术栈声明 (Python 3.13, pymavlink, asyncio, pydantic-settings)       │
  │  • 核心命名法 (snake_case, PascalCase, UPPER_SNAKE, 物理量带单位后缀)     │
  │  • 验证命令 (uv run ruff check, uv run mypy, uv run pytest)             │
  │  • 安全红线 (不删config, 不自动ARM, 不跳过Safety)                         │
  │  • ⚠️ 能力发现强制令 (实现前必须查 REGISTRY.md, 禁止重造轮子)             │
  │  • ⚠️ 变更流程约束 (新增功能前必须调用搜索能力进行代码调研，出具包含修改方案的规划，严禁盲目直接修改代码)     │
  │  • ↓ Skill 路由表 (编辑X模块时, 先读Y skill)                              │
  │                                                                           │
  └───────────────────────────────────────────────────────────────────────────┘
         │
         │ AGENTS.md 中的 Skill 路由表 → AI 知道"什么时候读什么skill"
         ▼
  ┌─── Tier 2: Module Skills  ──  模块级 (按需加载) ─────────────────────────┐
  │                                                                           │
  │  .agent/skills/                                                           │
  │  │                                                                        │
  │  │  ─── 跨模块: 能力注册表 (全局强制) ───                                  │
  │  ├── capability-registry/    # ⚠️ 能力清单 + 发现规范 + 注册流程          │
  │  │   ├── SKILL.md            #    使用规范 (何时查/何时注册)               │
  │  │   └── REGISTRY.md         #    能力清单 (Capability → Import路径)      │
  │  │                                                                        │
  │  │  ─── 模块级编码规范 ───                                                 │
  │  ├── core-fsm-rules/         # 状态机: 状态注册, 事件处理, 转换约束       │
  │  ├── comms-mavlink-rules/    # 通信: 消息枚举, 心跳, 超时, 连接生命周期   │
  │  ├── flight-control-rules/   # 飞行指令: 模式切换, 安全前置条件           │
  │  ├── payload-release-rules/  # 载荷: 释放协议, 弹道物理约束               │
  │  ├── vision-interface-rules/ # 视觉: 仅GPS坐标, 校验, Tracker规范        │
  │  ├── safety-monitor-rules/   # 安全: 检查优先级, 故障响应, Override       │
  │  ├── config-system-rules/    # 配置: 三层优先级, 平台检测, 校验           │
  │  └── testing-rules/          # 测试: SITL集成, 弹道已知答案, fixture     │
  │                                                                           │
  └───────────────────────────────────────────────────────────────────────────┘
         │
         │ Module Skill 可以 include 更精细的参考资源
         ▼
  ┌─── Tier 3: Reference Resources  ──  参考级 (skill内部按需) ──────────────┐
  │                                                                           │
  │  每个 Module Skill 可包含:                                                 │
  │  ├── SKILL.md                        # 主规范文档                         │
  │  ├── templates/                      # 完整模块脚手架 (照填即用)          │
  │  │   └── new_state.py.tpl            #   新增模块的完整骨架文件            │
  │  ├── examples/                       # "正确写法" 代码示例                │
  │  │   ├── good_state_impl.py          #   ✅ 示范                          │
  │  │   └── bad_state_impl.py           #   ❌ 反面教材 (成对出现更有效)     │
  │  └── resources/                      # 速查参考 (不含规范指令)            │
  │      ├── arduplane_modes.md                                               │
  │      └── mavlink_message_types.md                                         │
  │                                                                           │
  └───────────────────────────────────────────────────────────────────────────┘
```

## AGENTS.md Skill 路由表 — 关键设计

AGENTS.md 中包含一张路由表，指引 AI 编辑特定模块时先读取对应 Skill：

```
  ┌─────────────────────────────────────────────────────────────────────────┐
  │  # Module-Specific Standards (in AGENTS.md)                            │
  │                                                                         │
  │  Before editing code in any module below, you MUST first read          │
  │  the corresponding skill file for module-specific conventions:         │
  │                                                                         │
  │  | Source Path               | Governance Skill                    |   │
  │  |---------------------------|-------------------------------------|   │
  │  | src/striker/core/         | .agent/skills/core-fsm-rules/       |   │
  │  | src/striker/comms/        | .agent/skills/comms-mavlink-rules/  |   │
  │  | src/striker/flight/       | .agent/skills/flight-control-rules/ |   │
  │  | src/striker/payload/      | .agent/skills/payload-release-rules/|   │
  │  | src/striker/vision/       | .agent/skills/vision-interface-rules|   │
  │  | src/striker/safety/       | .agent/skills/safety-monitor-rules/ |   │
  │  | src/striker/config/       | .agent/skills/config-system-rules/  |   │
  │  | tests/                    | .agent/skills/testing-rules/        |   │
  │  | pkg/*/                    | .agent/skills/capability-registry/  |   │
  │  | src/striker/utils/        | .agent/skills/capability-registry/  |   │
  │                                                                         │
  │  Each skill contains:                                                   │
  │  - Module-specific naming conventions and type constraints              │
  │  - Registration patterns (how to add new components)                    │
  │  - Code examples of correct patterns                                    │
  │  - Integration points with other modules                                │
  │  - Common pitfalls and forbidden patterns                               │
  └─────────────────────────────────────────────────────────────────────────┘
```

## Module Skill 标准格式 (示例: comms-mavlink-rules)

```
  .agent/skills/comms-mavlink-rules/
  ├── SKILL.md                              # 主规范
  │   ---
  │   name: comms-mavlink-rules
  │   description: |
  │     Coding standards for the striker.comms module.
  │     Read this before editing any file in src/striker/comms/.
  │   metadata:
  │     scope: src/striker/comms/
  │     version: "1.0"
  │   ---
  │
  │   # comms/ Module Standards
  │
  │   ## Architecture Constraints
  │   - All MAVLink interaction MUST go through MAVLinkConnection
  │   - Never import pymavlink directly outside this module
  │   - All message types use constants from messages.py
  │
  │   ## Registration Pattern
  │   MSG_HANDLERS: dict[str, Callable] = {
  │       "HEARTBEAT": _handle_heartbeat,
  │       "GLOBAL_POSITION_INT": _handle_position,
  │   }
  │
  │   ## Forbidden Patterns
  │   - ❌ time.sleep() → ✅ asyncio.sleep()
  │   - ❌ Global mutable state
  │   - ❌ Silent exception swallowing
  │   - ❌ Direct serial/UDP access (use transport abstraction)
  │
  ├── templates/
  │   └── new_message_handler.py.tpl        # 新增消息处理器骨架
  ├── examples/
  │   └── good_heartbeat_handler.py         # 正确写法示范
  └── resources/
      └── arduplane_modes.md                # ArduPlane 模式枚举速查
```

## 行业最佳实践对齐

| 2025-2026 行业最佳实践 | Striker 方案 |
|---|---|
| 指令预算 <200条 | ✅ AGENTS.md <100行, skill 按需加载 |
| Command First (验证命令最顶部) | ✅ `uv run ruff` / `mypy` / `pytest` |
| Example-Driven (示例 > 描述) | ✅ 每个 skill 含 `templates/` + `examples/` |
| Zero Placeholder Policy | ✅ 红线: 禁止 `# TODO` 占位 |
| Interface-First (先接口再实现) | ✅ Protocol/ABC 先定义, skill 中要求 |
| Drift Monitoring (漂移监控) | ✅ 同一错误出现3次 → 加入对应 skill |
| Progressive Disclosure | ✅ 三层: 宪法 → 模块 → 参考 |
| Plan Mode First | ✅ OpenSpec proposal → design → tasks |
| `.local` 个人覆盖 | ✅ `AGENTS.local.md` (gitignored) |
| 反面教材成对出现 | ✅ `❌ bad` vs `✅ good` 对比格式 |
| Cross-Agent 兼容 | ✅ `.agents/skills/` 权威源 + IDE wrapper 按需生成 |
| Capability Registry (防重造轮子) | ✅ REGISTRY.md + L1 AI自律 + L2 CI lint |
| Code/Config/Data 三分离 | ✅ src/ 仅逻辑, config/ 仅参数, data/ 仅运行时产出 |

---

## 多 AI IDE 适配层

```
  ┌─────────────────────────────────────────────────────────────────────────┐
  │          Multi-IDE Adaptation Layer                                      │
  ├─────────────────────────────────────────────────────────────────────────┤
  │                                                                         │
  │  权威源 (Single Source of Truth):                                       │
  │  ┌────────────────────────────────────────────┐                         │
  │  │  .agents/skills/    → 厂商无关的 Skill 定义 │                        │
  │  │  AGENTS.md          → 宪法级规范            │                         │
  │  │  CHARTER.md         → 项目宪章              │                         │
  │  └────────────────────────────────────────────┘                         │
  │       │                                                                  │
  │       │ 按各 IDE 格式要求，生成 wrapper (可自动化)                        │
  │       ▼                                                                  │
  │  ┌────────────────────────┬─────────────────────────────────────┐       │
  │  │ IDE                    │ Wrapper 位置 + 格式                  │       │
  │  ├────────────────────────┼─────────────────────────────────────┤       │
  │  │ Cursor                 │ .cursor/rules/*.mdc                 │       │
  │  │ Claude Code            │ .claude/ (CLAUDE.md)                │       │
  │  │ Gemini                 │ .gemini/GEMINI.md                   │       │
  │  │ Codex                  │ .codex/ (AGENTS.md 自动识别)        │       │
  │  │ 其他 IDE               │ 按需新增, 内容引用权威源             │       │
  │  └────────────────────────┴─────────────────────────────────────┘       │
  │                                                                         │
  │  原则:                                                                  │
  │  • 规范只在权威源中编写和修改, wrapper 只做格式转换                      │
  │  • wrapper 内容 ≤ 权威源内容, 不得新增权威源中没有的规范                 │
  │  • wrapper 可引用权威源文件路径, 不必复制全文                            │
  │  • 初期仅维护 AGENTS.md + .agents/skills/, wrapper 按需生成             │
  │                                                                         │
  └─────────────────────────────────────────────────────────────────────────┘
```

---

## pkg/ — 冻结包治理 (uv workspace path dependency)

```
  ┌─────────────────────────────────────────────────────────────────────────┐
  │          pkg/ 整合方式: uv workspace + path dependency                  │
  ├─────────────────────────────────────────────────────────────────────────┤
  │                                                                         │
  │  每个 pkg/{name}/ 是独立 Python 包, 有自己的 pyproject.toml + version:  │
  │                                                                         │
  │  pkg/geodesy/pyproject.toml:                                            │
  │  ┌────────────────────────────────────────┐                             │
  │  │  [project]                             │                             │
  │  │  name = "geodesy"                      │                             │
  │  │  version = "1.2.0"                     │  ← 有自己的版本号           │
  │  │  requires-python = ">=3.13"            │                             │
  │  └────────────────────────────────────────┘                             │
  │                                                                         │
  │  项目根 pyproject.toml:                                                 │
  │  ┌────────────────────────────────────────┐                             │
  │  │  [project]                             │                             │
  │  │  dependencies = [                      │                             │
  │  │    "geodesy>=1.2.0,<2.0.0",           │  ← 版本约束                 │
  │  │  ]                                     │                             │
  │  │                                        │                             │
  │  │  [tool.uv.workspace]                   │                             │
  │  │  members = ["pkg/*"]                   │  ← uv workspace 管理       │
  │  │                                        │                             │
  │  │  [tool.uv.sources]                     │                             │
  │  │  geodesy = { workspace = true }        │  ← 指向本地 workspace 成员  │
  │  └────────────────────────────────────────┘                             │
  │                                                                         │
  │  效果:                                                                  │
  │  • import geodesy → 正常工作, 和导入 pymavlink 体验一致                │
  │  • uv lock 时如果版本不匹配 → 原生报错, 阻塞安装                       │
  │  • 升级 = 更新 pkg/{name}/ 源码 + 改 version 字段 + uv lock            │
  │                                                                         │
  └─────────────────────────────────────────────────────────────────────────┘
```

### 依赖方向与边界

```
  src/striker/  ──── import ────▶  pkg/         (允许)
  src/striker/  ──── import ────▶  pymavlink    (允许, 仅 comms/ 内)
  pkg/          ──── import ────▶  src/striker/ (禁止! 单向依赖)
  pkg/a/        ──── import ────▶  pkg/b/       (禁止! 包间无依赖)
```

### Conflict 检测流程

```
  开发者/AI 修改 pkg/{name}/ 代码
       │
       ├──── 更新了 version? ────── 否 ──▶ CI lint 报错:
       │                                  "pkg/{name} source changed
       │                                   but version not bumped"
       │
       ├──── version 满足根 pyproject.toml 约束?
       │     ├── 是 ──▶ uv lock 成功 ✓
       │     └── 否 ──▶ uv lock 报 CONFLICT, 阻塞:
       │               "geodesy 0.9.0 does not satisfy >=1.2.0"
       │
       └──── REGISTRY.md 条目更新? ──── CI lint 检查 ✓
```

### REGISTRY.md 同步保障 (L1 + L2)

```
  ┌─── L1: AI 自律 (零成本) ──────────────────────────────────────────────┐
  │                                                                         │
  │  AGENTS.md 宪法级规则:                                                   │
  │  "新增通用能力后必须注册到 REGISTRY.md"                                  │
  │  OpenSpec tasks 可包含显式的 "更新 REGISTRY.md" 子任务                  │
  │                                                                         │
  └─────────────────────────────────────────────────────────────────────────┘
  ┌─── L2: CI Lint 兜底 (自动) ──────────────────────────────────────────┐
  │                                                                         │
  │  scripts/lint_registry.py:                                              │
  │  1. AST 扫描 utils/*.py + pkg/*/__init__.py 的公共符号                  │
  │     (无 _ 前缀的 def/class)                                             │
  │  2. 解析 REGISTRY.md 中已注册的 import 路径                             │
  │  3. diff → 未注册的公共符号 = CI 报错, 阻塞合入                         │
  │                                                                         │
  │  scripts/check_pkg_versions.py:                                         │
  │  1. 遍历 pkg/**/pyproject.toml 的 version                              │
  │  2. 和根 pyproject.toml dependencies 做交叉比对                         │
  │  3. 版本不匹配 → CI 报错 + 清晰的升级指引                              │
  │                                                                         │
  └─────────────────────────────────────────────────────────────────────────┘
```

---

# Part 4: 项目工程规范

## Tier 1 详细设计: CHARTER.md + AGENTS.md

### CHARTER.md — 项目宪章

```
  ┌─────────────────────────────────────────────────────────────────┐
  │                       项目 OKR                                   │
  ├─────────────────────────────────────────────────────────────────┤
  │                                                                 │
  │  O1: 安全可靠的自动飞行控制                                      │
  │  ─────────────────────────                                      │
  │  KR1: 人工 Override 响应延迟 < 100ms                            │
  │  KR2: 安全监控覆盖 100% 的飞行状态                              │
  │  KR3: 所有异常状态有明确的 fallback 行为                         │
  │  KR4: 地理围栏越界检测 100% 可靠                                │
  │                                                                 │
  │  O2: 精确的投弹命中率                                            │
  │  ─────────────────────                                          │
  │  KR1: 无风条件下投弹 CEP < 5m                                   │
  │  KR2: 弹道解算延迟 < 50ms                                      │
  │  KR3: 支持实时风速补偿                                           │
  │                                                                 │
  │  O3: 高可维护性和可配置性                                        │
  │  ──────────────────────                                         │
  │  KR1: 三层配置优先级 100% 覆盖所有参数                           │
  │  KR2: 新增任务阶段 < 1天开发量                                  │
  │  KR3: RPi5↔Orin 切换仅需改配置，零代码修改                      │
  │  KR4: SITL 测试覆盖 100% 的状态转换                             │
  │                                                                 │
  │  O4: 卓越的 AI 协同开发体验                                      │
  │  ──────────────────────                                         │
  │  KR1: AI Agent 理解项目并产出合规代码的首次通过率 > 90%          │
  │  KR2: 所有模块有注册机制，AI 可通过约定发现和扩展               │
  │  KR3: 每个公共接口有完整 docstring 和类型注解                   │
  │                                                                 │
  │  Red Lines:                                                     │
  │  ────────                                                       │
  │  • 不暴露远程控制接口到公网                                      │
  │  • 不在没有 Safety Monitor 的情况下执行 ARM                     │
  │  • 不信任未校验的坐标数据                                       │
  │  • 不在 Override 后自动恢复自动飞行                              │
  │  • 不重造轮子: 实现前必须查 REGISTRY.md                         │
  │  • 不在业务模块内写孤立工具函数 (放 utils/ 或 pkg/)             │
  └─────────────────────────────────────────────────────────────────┘
```

### AGENTS.md — AI 宪法级规范 (Tier 1, <100行)

> 以下内容在 AGENTS.md 中精简保留核心条目。
> 模块级详细规范（如 MAVLink 消息处理约定、状态机注册模式等）
> 拆分到 `.agent/skills/{module}-rules/SKILL.md` 中按需加载。

```
  ┌─────────────────────────────────────────────────────────────────────────┐
  │                   AGENTS.md 宪法级内容蓝图                                │
  │                   (精简到 <100行, 模块细节见 Tier 2 Skills)               │
  ├─────────────────────────────────────────────────────────────────────────┤
  │                                                                         │
  │  1. 命名规范                                                            │
  │  ━━━━━━━━━━                                                            │
  │  变量/函数:    snake_case         target_lat, calculate_drop_point()    │
  │  类名:         PascalCase         MissionStateMachine, FlightController │
  │  常量:         UPPER_SNAKE_CASE   MAX_LOITER_TIME_S, DEFAULT_BAUD_RATE │
  │  私有:         _leading_underscore _mavlink_conn, _process_heartbeat() │
  │  模块文件:     snake_case.py      state_machine.py, mavlink_conn.py    │
  │  配置键:       snake_case         serial_port, loiter_radius_m         │
  │  环境变量:     STRIKER_ 前缀      STRIKER_SERIAL_PORT                   │
  │                                                                         │
  │  物理量变量必须带单位后缀:                                                │
  │  ┌───────────────────────────────────────────────────────────┐         │
  │  │  altitude_m      (米)                                     │         │
  │  │  speed_mps       (米/秒)                                  │         │
  │  │  distance_m      (米)                                     │         │
  │  │  angle_deg       (度)                                     │         │
  │  │  angle_rad       (弧度)                                   │         │
  │  │  time_s          (秒)                                     │         │
  │  │  time_ms         (毫秒)                                   │         │
  │  │  pressure_pa     (帕斯卡)                                 │         │
  │  │  pwm_us          (微秒, PWM脉宽)                          │         │
  │  │  lat_deg / lon_deg  (度, WGS84)                           │         │
  │  │  lat_degE7 / lon_degE7  (度×10^7, MAVLink整数格式)        │         │
  │  └───────────────────────────────────────────────────────────┘         │
  │                                                                         │
  │  2. 类型注解                                                            │
  │  ━━━━━━━━━                                                             │
  │  • 所有函数参数和返回值必须有类型注解                                     │
  │  • 使用 Python 3.13 内建泛型: list[str], dict[str, int]                │
  │  • 复杂类型使用 TypeAlias 或 NewType                                    │
  │  • 坐标类型定义:                                                        │
  │    LatDeg: TypeAlias = float                                            │
  │    LonDeg: TypeAlias = float                                            │
  │    AltitudeM: TypeAlias = float                                         │
  │                                                                         │
  │  3. Docstring 格式 (Google Style)                                      │
  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━                                        │
  │  def calculate_release_point(                                           │
  │      target_lat_deg: float,                                             │
  │      target_lon_deg: float,                                             │
  │      altitude_m: float,                                                 │
  │      ground_speed_mps: float,                                           │
  │      heading_deg: float,                                                │
  │      wind_speed_mps: float,                                             │
  │      wind_dir_deg: float,                                               │
  │  ) -> tuple[float, float]:                                              │
  │      """Calculate the optimal bomb release point.                       │
  │                                                                         │
  │      Uses ballistic trajectory model with wind compensation             │
  │      to determine the GPS coordinates where payload should              │
  │      be released to hit the target.                                     │
  │                                                                         │
  │      Args:                                                              │
  │          target_lat_deg: Target latitude in decimal degrees.             │
  │          target_lon_deg: Target longitude in decimal degrees.            │
  │          altitude_m: Release altitude above target in meters.            │
  │          ground_speed_mps: Aircraft ground speed in m/s.                │
  │          heading_deg: Aircraft heading in degrees (0=N, 90=E).          │
  │          wind_speed_mps: Wind speed in m/s (from EKF).                  │
  │          wind_dir_deg: Wind direction in degrees (where FROM).          │
  │                                                                         │
  │      Returns:                                                           │
  │          Tuple of (release_lat_deg, release_lon_deg).                   │
  │                                                                         │
  │      Raises:                                                            │
  │          ValueError: If altitude_m <= 0 or speed <= 0.                  │
  │      """                                                                │
  │                                                                         │
  │  4. 模块注册约定                                                        │
  │  ━━━━━━━━━━━━━━                                                        │
  │  • 状态类: 在 core/states/ 目录下创建，在 __init__.py 中注册             │
  │  • 视觉接收器: 实现 VisionReceiver Protocol, 在 vision/__init__.py 注册 │
  │  • 释放控制器: 实现 ReleaseController Protocol, 在 payload/__init__.py  │
  │  • 所有可插拔组件通过 config 选择，通过 registry 字典查找:              │
  │                                                                         │
  │    RELEASE_REGISTRY: dict[str, type[ReleaseController]] = {             │
  │        "mavlink": MavlinkServoRelease,                                  │
  │        "gpio": GpioRelease,                                             │
  │    }                                                                    │
  │                                                                         │
  │    def get_release_controller(method: str) -> ReleaseController:        │
  │        cls = RELEASE_REGISTRY[method]                                   │
  │        return cls(settings)                                             │
  │                                                                         │
  │  5. 日志规范                                                            │
  │  ━━━━━━━━━                                                             │
  │  • 使用 structlog 结构化日志                                             │
  │  • 禁止 print() (ruff规则强制)                                          │
  │  • 日志级别约定:                                                        │
  │    DEBUG:   MAVLink 消息收发细节                                         │
  │    INFO:    状态转换、关键事件                                           │
  │    WARNING: 非致命异常、降级行为                                         │
  │    ERROR:   需要人工关注的错误                                           │
  │    CRITICAL: 安全相关、紧急降落                                          │
  │                                                                         │
  │  每条日志必须包含:                                                       │
  │    log.info("state_transition",                                         │
  │         from_state="LOITER",                                            │
  │         to_state="ENROUTE",                                             │
  │         trigger="target_received",                                      │
  │         target_lat=30.xxx,                                              │
  │         target_lon=120.xxx,                                             │
  │         elapsed_s=12.3,                                                 │
  │    )                                                                    │
  │                                                                         │
  │  6. 错误处理                                                            │
  │  ━━━━━━━━━                                                             │
  │  • 自定义异常层次:                                                      │
  │    StrikerError (基类)                                                  │
  │      ├── ConnectionError (连接失败)                                     │
  │      ├── SafetyError (安全检查失败)                                     │
  │      ├── NavigationError (导航计算错误)                                 │
  │      ├── ReleaseError (投弹释放失败)                                    │
  │      └── ConfigError (配置错误)                                         │
  │  • 任何异常不能导致静默失败                                              │
  │  • 飞行安全相关异常 → 立即切 EMERGENCY                                  │
  │                                                                         │
  │  7. 测试约定                                                            │
  │  ━━━━━━━━━                                                             │
  │  • 单元测试: tests/unit/ , pytest                                      │
  │  • SITL集成: tests/integration/ , 需要ArduPilot SITL运行               │
  │  • 命名: test_{module}_{function}_{scenario}                           │
  │  • 弹道计算必须有已知答案测试 (known-answer test)                       │
  │                                                                         │
  │  8. Import 顺序                                                        │
  │  ━━━━━━━━━━━                                                           │
  │  # 标准库                                                               │
  │  import asyncio                                                         │
  │  import math                                                            │
  │                                                                         │
  │  # 第三方                                                               │
  │  from pymavlink import mavutil                                          │
  │  from pydantic_settings import BaseSettings                             │
  │                                                                         │
  │  # 本项目: 冻结包 (pkg/)                                                │
  │  from geodesy import vincenty_distance                                  │
  │                                                                         │
  │  # 本项目: 业务代码 (src/striker/)                                      │
  │  from striker.core.events import TargetReceived                         │
  │  from striker.comms.mavlink_conn import MAVLinkConnection               │
  │                                                                         │
  │  (ruff isort 强制)                                                      │
  │                                                                         │
  │  9. 能力发现 (Capability Discovery)                                     │
  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━                                        │
  │  • 实现任何功能前, 必须先查 REGISTRY.md                                  │
  │  • 已注册的能力 → import 使用, 禁止重新实现                              │
  │  • 新增通用能力 → 放置在正确位置 (utils/ 或 pkg/)                       │
  │    并追加注册到 REGISTRY.md                                              │
  │  • 禁止在 states/*.py 或 app.py 中编写孤立工具函数                      │
  │  • 详见: .agent/skills/capability-registry/SKILL.md                     │
  │                                                                         │
  │  10. Code / Config / Data 分离                                          │
  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━                                        │
  │  • Code: src/ — 仅执行逻辑, 禁止硬编码路径或数据                         │
  │  • Config: config.json / 环境变量 — 仅环境差异参数                       │
  │  • Data: data/ — 所有运行时产出 (飞行记录/遥测/输出)                     │
  │  • 禁止运行时数据散落在项目根目录或 src/ 内                              │
  │                                                                         │
  └─────────────────────────────────────────────────────────────────────────┘
```

---

## 精化项目结构 (最终版)

```
striker/                                 # 项目根
├── CHARTER.md                           # 项目宪章 (OKR + Red Lines)
├── AGENTS.md                            # AI Agent 宪法级规范 (<100行)
├── AGENTS.local.md                      # 个人覆盖 [gitignored]
├── README.md                            # 人类开发者文档
├── pyproject.toml                       # uv 项目配置 + ruff/mypy
├── uv.lock                             # 锁定依赖
├── config.json                          # 用户配置 (Layer 2) [gitignored]
├── config.example.json                  # 配置模板 [提交到git]
├── .env.example                         # 环境变量模板
├── .python-version                      # 3.13
│
├── .agent/                              # AI 治理 + 工作流
│   ├── skills/
│   │   │
│   │   │  ─── Tier 2: 模块编码规范 Skills ───
│   │   ├── core-fsm-rules/
│   │   │   ├── SKILL.md                 # 状态机模块规范
│   │   │   └── examples/
│   │   │       └── good_state_impl.py   # 正确写法示范
│   │   ├── comms-mavlink-rules/
│   │   │   ├── SKILL.md                 # MAVLink 通信规范
│   │   │   ├── examples/
│   │   │   │   └── good_heartbeat.py
│   │   │   └── resources/
│   │   │       └── arduplane_modes.md   # 模式枚举速查
│   │   ├── flight-control-rules/
│   │   │   └── SKILL.md                 # 飞行指令规范
│   │   ├── payload-release-rules/
│   │   │   ├── SKILL.md                 # 载荷投弹规范
│   │   │   └── resources/
│   │   │       └── ballistic_formulas.md
│   │   ├── vision-interface-rules/
│   │   │   └── SKILL.md                 # 视觉接口规范
│   │   ├── safety-monitor-rules/
│   │   │   └── SKILL.md                 # 安全监控规范
│   │   ├── config-system-rules/
│   │   │   └── SKILL.md                 # 配置系统规范
│   │   ├── testing-rules/
│   │   │   ├── SKILL.md                 # 测试规范
│   │   │   └── examples/
│   │   │       └── good_sitl_test.py
│   │   │
│   │   │  ─── 跨模块: 能力注册表 ───
│   │   ├── capability-registry/
│   │   │   ├── SKILL.md                 # 能力发现与注册规范
│   │   │   └── REGISTRY.md              # 能力清单 (能力→Import路径)
│   │   │
│   │   │  ─── OpenSpec 工作流 Skills (已有) ───
│   │   ├── openspec-apply-change/
│   │   ├── openspec-explore/
│   │   ├── openspec-new-change/
│   │   ├── openspec-propose/
│   │   └── ...
│   │
│   └── workflows/                       # OpenSpec 工作流定义
│
├── .claude/                             # Claude wrapper (引用 .agents/ 权威源)
├── .codex/                              # Codex wrapper (引用 .agents/ 权威源)
├── openspec/                            # OpenSpec 变更管理
│
├── src/
│   └── striker/                         # 主包
│       ├── __init__.py                  # 版本号
│       ├── __main__.py                  # python -m striker 入口
│       ├── app.py                       # asyncio 主循环编排
│       ├── exceptions.py               # 自定义异常层次
│       │
│       ├── core/                        # ─── 核心: 状态机引擎 ───
│       │   ├── __init__.py
│       │   ├── machine.py               # FSM 引擎 (泛化)
│       │   ├── context.py               # 任务上下文 (共享状态)
│       │   ├── events.py                # 事件枚举和数据类
│       │   └── states/                  # 各状态实现
│       │       ├── __init__.py          # ← 状态注册表
│       │       ├── base.py              # BaseState ABC
│       │       ├── init.py
│       │       ├── preflight.py
│       │       ├── takeoff.py
│       │       ├── loiter.py
│       │       ├── enroute.py
│       │       ├── approach.py
│       │       ├── release.py
│       │       ├── return_base.py
│       │       ├── landing.py
│       │       ├── completed.py
│       │       ├── override.py          # 人工接管终态
│       │       └── emergency.py         # 紧急降落
│       │
│       ├── comms/                       # ─── 通信层 ───
│       │   ├── __init__.py
│       │   ├── connection.py            # MAVLink 连接管理
│       │   ├── messages.py              # 消息收发封装
│       │   ├── heartbeat.py             # 心跳管理 + 连接健康
│       │   └── telemetry.py             # 遥测数据解析/缓存
│       │
│       ├── flight/                      # ─── 飞行指令层 ───
│       │   ├── __init__.py
│       │   ├── controller.py            # 高级指令 (arm/goto/mode...)
│       │   ├── modes.py                 # ArduPlane 模式枚举
│       │   ├── navigation.py            # 航线/航点管理
│       │   └── landing_sequence.py      # 降落序列生成+上传
│       │
│       ├── payload/                     # ─── 载荷系统 ───
│       │   ├── __init__.py              # ← 释放控制器注册表
│       │   ├── protocol.py              # ReleaseController Protocol
│       │   ├── ballistics.py            # 弹道解算引擎
│       │   ├── mavlink_release.py       # DO_SET_SERVO 实现
│       │   ├── gpio_release.py          # GPIO 实现
│       │   └── models.py                # 物理参数数据类
│       │
│       ├── vision/                      # ─── 视觉链路 ───
│       │   ├── __init__.py              # ← 接收器注册表
│       │   ├── protocol.py              # VisionReceiver Protocol
│       │   ├── tcp_receiver.py          # TCP 实现
│       │   ├── udp_receiver.py          # UDP 实现
│       │   ├── models.py                # GpsTarget 数据类 + 校验
│       │   └── tracker.py               # TargetTracker (自适应频率)
│       │
│       ├── safety/                      # ─── 安全监控 ───
│       │   ├── __init__.py
│       │   ├── monitor.py               # SafetyMonitor (主循环)
│       │   ├── checks.py                # 各项检查实现
│       │   ├── geofence.py              # 地理围栏
│       │   └── override_detector.py     # Override 检测
│       │
│       ├── telemetry/                   # ─── 遥测日志 ───
│       │   ├── __init__.py
│       │   ├── logger.py                # structlog 配置
│       │   ├── flight_recorder.py       # 飞行数据 CSV 记录
│       │   └── reporter.py              # GCS 状态上报
│       │
│       ├── config/                      # ─── 配置管理 ───
│       │   ├── __init__.py
│       │   ├── settings.py              # pydantic-settings 主配置
│       │   ├── platform.py              # 平台检测 (RPi5/Orin/SITL)
│       │   └── validators.py            # 配置校验器
│       │
│       └── utils/                       # ─── 工具 (自研, 活跃) ───
│           ├── __init__.py
│           ├── geo.py                   # GPS/坐标计算
│           ├── converter.py             # CoordConverter (公共坐标转换库)
│           ├── units.py                 # 单位转换
│           └── timing.py               # 精确计时
│
├── pkg/                                 # ─── 冻结代码区 (uv workspace) ───
│   ├── README.md                        # 此目录为何只读 + 使用规则
│   └── {vendor_package}/                # 每个包 = 独立 Python 包
│       ├── pyproject.toml               # name + version (版本管理!)
│       ├── README.md                    # 来源/license/公共API摘要
│       ├── src/{package_name}/          # src layout
│       │   ├── __init__.py
│       │   └── ...                      # 包内代码
│       └── tests/                       # 包自身的测试 (可选)
│
├── tests/
│   ├── conftest.py                      # pytest fixtures
│   ├── unit/
│   │   ├── test_ballistics.py           # 弹道已知答案测试
│   │   ├── test_state_machine.py
│   │   ├── test_config.py
│   │   ├── test_geo.py
│   │   └── test_tracker.py
│   ├── integration/
│   │   ├── test_sitl_connection.py
│   │   ├── test_sitl_takeoff.py
│   │   └── test_sitl_full_mission.py
│   └── fixtures/
│       └── sample_config.json
│
├── data/                                # ─── 运行时数据 (gitignored) ───
│   ├── flight_records/                  # 飞行数据 CSV 记录
│   ├── telemetry_logs/                  # 遥测日志存档
│   └── mission_outputs/                 # 任务产出 (投弹点计算结果等)
│
├── scripts/
│   ├── setup_rpi5.sh
│   ├── setup_orin.sh                    # 预留
│   ├── run_sitl.sh
│   ├── preflight_check.py              # 独立预飞检查脚本
│   ├── lint_registry.py                # CI: 公共符号↔REGISTRY.md 同步检查
│   └── check_pkg_versions.py           # CI: pkg/ 版本一致性检查
│
└── docs/
    ├── architecture.md
    ├── wiring.md                        # CUAV X7 ↔ RPi5 接线
    ├── config_reference.md              # 配置参数完整参考
    ├── states.md                        # 状态机文档
    └── ballistics.md                    # 弹道模型数学推导
```

---

## 工具链

```
  ┌────────────────────────────────────────────────────────────────┐
  │                    开发工具链                                    │
  ├──────────────┬─────────────────────────────────────────────────┤
  │ 包管理器      │ uv (取代 pip/poetry, Rust编写, 极速)           │
  │ Python       │ 3.13 (via uv python install 3.13)              │
  │ Linter       │ ruff (lint + format + isort 一体化)            │
  │ 类型检查     │ mypy --strict                                   │
  │ 测试         │ pytest + pytest-asyncio                         │
  │ 日志         │ structlog                                       │
  │ 配置         │ pydantic-settings (三层优先级)                   │
  │ 数据类       │ pydantic v2 (配置/消息的验证)                    │
  │ MAVLink      │ pymavlink                                       │
  │ GPIO (可选)  │ gpiod (RPi5) / Jetson.GPIO (Orin)              │
  │ SITL测试     │ ArduPilot SITL + MAVProxy                       │
  │ CI           │ GitHub Actions (lint/test/SITL)                 │
  │ CI: Registry │ lint_registry.py (公共符号同步检查)              │
  │ CI: Pkg版本  │ check_pkg_versions.py (版本一致性检查)           │
  │ Workspace    │ uv workspace (pkg/* 本地包管理)                  │
  │ AI 治理      │ AGENTS.md + CHARTER.md + OpenSpec               │
  └──────────────┴─────────────────────────────────────────────────┘
```

---

# Part 5: 风险与下一步

## 已知风险和待探索项

| 风险/未知 | 影响 | 建议 |
|-----------|------|------|
| ArduPlane 自动降落需要预设航线 | 降落阶段设计复杂 | 起飞前上传降落序列到飞控 |
| ~~pymavlink 与 Python 3.14 兼容性~~ | ~~已规避~~ | ✅ 改用 3.13，无兼容风险 |
| 投弹精度严重依赖风速估算 | 解算误差 | 先用简单模型 + 实飞标定修正系数 |
| RPi5 GPIO vs Orin GPIO API 不同 | 平台切换 | gpiod 统一抽象 (Linux通用) |
| 视觉链路传入格式必须为GPS绝对坐标 | 上游需自行转换 | CoordConverter 工具库供上游调用 |
| LOITER 半径受限于飞行器最小转弯半径 | 不能设太小 | config 可调, 默认 80m |
| 空速管故障 = 固定翼致命 | 安全 | 必须在 PREFLIGHT 检测 |

---

## 下一步？

这个架构蓝图已经相当全面了。我看到几条可能的路径：

1. **直接落地** — 我把这些决策固化成 OpenSpec proposal，然后开始搭建项目脚手架？
2. **继续深挖某个子系统** — 比如弹道解算的数学模型，或者降落序列的具体实现？
3. **先做技术验证** — 比如先搭建 SITL 环境，验证 pymavlink + Python 3.13 + ArduPlane 的连通性？

你想走哪条路？或者有哪个部分你觉得还需要更多讨论？